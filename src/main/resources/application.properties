# Nombre de la aplicación
spring.application.name=sistema-pagos-back
server.port=8084
# Configuración de la base de datos H2 (en memoria)
spring.datasource.url=jdbc:mysql://localhost/estudintes-db
spring.datasource.username=root
spring.datasource.password=root
# spring.datasource.driver-class-name=org.h2.Driver

# Habilitar consola H2
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console 

# Configuración JPA
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# spring.jpa.show-sql=true            
# spring.jpa.properties.hibernate.format_sql=true

# Formato de fechas para Spring MVC
spring.mvc.format.date=dd/MM/yyyy

# Swagger UI
springdoc.swagger-ui.path=/swagger-ui.html


#Jwt

jwt.secret = secret
jwt.expiration = 3600


# application.properties

# Nivel de logging general
logging.level.root=INFO
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.boot.autoconfigure=ERROR

# Patrón de logging con colores
logging.pattern.console=%clr(%d{yyyy-MM-dd HH:mm:ss}){faint} \
%clr(%-5p){yellow} \
%clr(---){faint} \
%clr([%15.15t]){faint} \
%clr(%-40.40logger{39}){cyan} : \
%clr(%m){white}%n%wEx
